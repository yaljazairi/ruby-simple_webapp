set -e -x

cp -a rubyapp/* ${BOSH_INSTALL_TARGET}
cd $BOSH_INSTALL_TARGET

#source /var/vcap/packages/ruby-2.6.7-r0.42.0/bosh/runtime.env
source /var/vcap/packages/ruby-2.6.7-r0.42.0/bosh/compile.env

bundle install \
  --local \
  --deployment \
  --without development test


#mkdir -p ${BOSH_INSTALL_TARGET}
#rm -rf ${BOSH_INSTALL_TARGET}/* # in case reuse of compilation VM left an old version here
#cp -Ra rubyapp/* ${BOSH_INSTALL_TARGET}
#cd $BOSH_INSTALL_TARGET

#bosh_bundle
#bosh_generate_runtime_env